#!/bin/bash

#SBATCH -J GMXem

##  partition (queue)
##SBATCH -p cascade
##SBATCH -p knl
#SBATCH -p flnr-ice
##SBATCH -p flnr-sod

## max. execution time
##SBATCH -t 7-00:00:00
#SBATCH -t 4-00:00:00

##SBATCH --exclusive

#SBATCH --mem=32GB
##SBATCH --mem=64GB
##SBATCH --mem=164GB
##SBATCH --mem=16GB

# do not restart in the case of nodefail!
#SBATCH --no-requeue
#SBATCH --no-kill

#SBATCH -N 1 -n 8
#SBATCH  --sockets-per-node=1

## memory NO !!!
##SBATCH --mem-per-cpu=28G

## stdout/stderr output file
##SBATCH -o log_slurm_job.%j.%N.std_out_err
#SBATCH -o log_slurm_job.%j.%N.std_out

## mail
#SBATCH --mail-user=milias@theor.jinr.ru
#SBATCH --mail-type=ALL


echo -e "\n Job user is $SLURM_JOB_USER and his job $SLURM_JOB_NAME has assigned ID $SLURM_JOBID"
echo This job was submitted from the computer $SLURM_SUBMIT_HOST
echo and from the home directory:
echo $SLURM_SUBMIT_DIR
echo
echo It is running on the cluster compute node:
echo $SLURM_CLUSTER_NAME
echo and is employing $SLURM_JOB_NUM_NODES node/nodes:
echo $SLURM_JOB_NODELIST
echo
echo -e "Job partition is $SLURM_JOB_PARTITION \n"
echo The job requests $SLURM_CPUS_ON_NODE CPU per task.

#echo "modules at disposal:"
#module avail
#echo
#
module purge
module add GVR/v1.0-1
module add Python/v3.10.13
module add openmpi/v5.0.7_gcc1230
module add fftw/v3.3.10_gcc1120
module add LAPACK/v3.12.0_gcc1230
echo -e "\n\n loaded modules :"
module list

# 
  echo -e "\n\n*** hydra.jinr.ru setting for GROMACS run:   ***"
  echo -e "gcc ?"; which gcc; gcc --version
  echo -e "g++ ?"; which g++; g++ --version
  echo -e "mpicxx ?"; which mpicxx; mpicxx --version
  echo -e "mpirun ?";  which mpirun; mpirun --version

  export GROMACS=/zfs/scratch/HybriLITWorkshop2025/milias/software/gromacs/git_cloned/gromacs
  export BUILD=build_openmpi


  echo -e "\n GROMACS=$GROMACS"
  echo -e "\n\n ldd $GROMACS/$BUILD/bin/gmx_mpi :"
  ldd $GROMACS/$BUILD/bin/gmx_mpi

  echo -e "\n\n $GROMACS/$BUILD/bin/gmx_mpi -h :"
  $GROMACS/$BUILD/bin/gmx_mpi -h

  echo -e "\n\n Own GROMACS energy minimization run :"
  $GROMACS/$BUILD/bin/gmx_mpi mdrun -v -deffnm em

  exit 0
