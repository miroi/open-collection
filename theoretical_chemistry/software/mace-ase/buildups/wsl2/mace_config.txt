# MACE-ML Configuration File
# Lines starting with '#' are comments

# Path to MACE models directory
MACE_MODELS_PATH = ~/.cache/mace

# Default model to use (name of the model file)
DEFAULT_MODEL = macempa0mediummodel

# Default device (cpu or cuda)
DEFAULT_DEVICE = cpu
